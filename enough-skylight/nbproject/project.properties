auxiliary.org-netbeans-modules-projectimport-eclipse-core.key=src=src/main;src=test/main;prj=enough-polish-rhino;prj=enough-polish-j2me;file=lib/cldc-1.1.jar;file=lib/midp-2.1.jar;output=bin/main;
auxiliary.org-netbeans-modules-projectimport-eclipse-core.project=.
auxiliary.org-netbeans-modules-projectimport-eclipse-core.timestamp=1274800588000
auxiliary.org-netbeans-modules-projectimport-eclipse-core.workspace=..
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
buildfile=nb-build.xml
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/enough-skylight.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.cldc-1.1.jar=lib/cldc-1.1.jar
file.reference.midp-2.1.jar=lib/midp-2.1.jar
file.reference.src-main=src/main
file.reference.test-main=test/main
includes=**
jar.compress=false
javac.classpath=\
    ${reference.enough-polish-rhino.jar}:\
    ${reference.enough-polish-j2me.jar}:\
    ${file.reference.cldc-1.1.jar}:\
    ${file.reference.midp-2.1.jar}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:fallthrough -Xlint:finally -Xlint:serial
javac.deprecation=true
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jaxbwiz.endorsed.dirs="${netbeans.home}/../ide12/modules/ext/jaxb/api"
main.class=
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
project.enough-polish-j2me=../enough-polish-j2me
project.enough-polish-rhino=../enough-polish-rhino
reference.enough-polish-j2me.jar=${project.enough-polish-j2me}/dist/enough-polish-j2me.jar
reference.enough-polish-rhino.jar=${project.enough-polish-rhino}/nbdist/enough-polish-rhino.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=${file.reference.src-main}
test.src.dir=${file.reference.test-main}
