package de.enough.skylight.css;

import de.enough.skylight.dom.Document;

/**
 * This object will first add all CSS rules to the elements in the document. Then it will apply all inline styles
 * of the element to the element and then the properties are checked for validity.
 * @author rickyn
 *
 */
public class CssApplyer {

	public void applyCss(StyleSheet styleSheet, Document document) {
		
	}
}
