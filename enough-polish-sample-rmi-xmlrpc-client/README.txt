Usage:

- adjust the settings in the build.xml file or use a username.properties file
- build and deploy the server e.g. 'ant clean deploy'
- by default your server can be reached at http://localhost:8080/xmlrpcserver/xmlrpc
- build ant run the midlet using 'ant emulator'
- the midlet will call the server to perform to operations
 